query getDevicesOfGroup($groupId: String!) {
  group(id: $groupId) {
    id
    devices {
      id
      name
      category
      fn {
        power {
          relay1
          relay2
          relay3
        }
        sensor {
            sensor1
        }
      }
    }
  }
}
